import SafariContentView from "../components/SafariContentView";

const SafariContentSection = ({ socials, projects }) => {
  return <SafariContentView socials={socials} projects={projects} />;
};

export default SafariContentSection;
